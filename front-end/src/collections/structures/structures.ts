export const createNewStructure = (name: string, description: string) => {
  // Create a new structure and then return the id of the new structure
  return 3; //TODO: This should not be hard coded
};
