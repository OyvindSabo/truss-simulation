import styled from 'styled-components';
import { NAVIGATOR_WIDTH } from '../../constants/sizes';

export const ViewContainer = styled.div`
  position: absolute;
  top: 0;
  width: 100%;
  margin-left: ${NAVIGATOR_WIDTH}px;
`;
