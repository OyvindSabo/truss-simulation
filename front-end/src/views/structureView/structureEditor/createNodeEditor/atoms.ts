import styled from 'styled-components';
import { BUTTON_HEIGHT } from '../../../../constants/config/sizes';

export const SwitchContainer = styled.div`
  position: relative;
  top: 0;
  right: 0;
  height: ${BUTTON_HEIGHT}px;
`;
